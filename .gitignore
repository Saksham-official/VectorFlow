# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Environment / secrets
.env

# OS / IDE
.DS_Store
.idea/
.vscode/

# Runtime data
data/uploads/
data/blockchain/

# Allow benchmark model artifacts (trained weights for evaluation suite)
!benching/models/*.pt
!benching/models/*.joblib
!benching/models/*.ubj

# Allow production LSTM artifacts (already tracked, explicitly kept)
!lstm/artifacts/*.pt
!lstm/artifacts/*.joblib

# Datasets (large, regenerate with modules/data_pipeline/build_training_set.py)
data/*.csv
data/*.parquet
data/raw/
data/*.README.md
wheels/
.playwright-mcp/
